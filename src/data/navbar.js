import { useNavigate } from "react-router-dom";

const navbar = {
  tabs: [
    { id: "home", button: "Home" },
    { id: "gallery", button: "Gallery", onClick: () => {} },
    {
      id: "attractions",
      button: "Attractions",
      onClick: () => {},
    },
  ],
};

export default navbar;
